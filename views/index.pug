extends layout.pug 

block title 
    title Unscrabble | Anagram Generator

block nav
    a.float-right(href="https://github.com/AhamSammich/unscrabble" target="_blank") 
        .flex-row.flex-center
            p View project at GitHub.com/AhamSammich
            img(src="./images/logos/GitHub-Mark-64px.png" alt="GitHub"
            style="height: 35px; width: 35px;")

block header
    .tile-set.flex-row(style="padding-top: 10%;")
        each char in "unscrabble"
            .tile(
                style=`background-image: url('./images/tiles/${
                    char.toLowerCase()}.png'); background-size: cover;`
            )
    h2(style="letter-spacing: 2px;") Anagram Generator

block main
    main.flex-col.flex-center
        form.flex-row.flex-even(action="/results" method="POST" style="gap: 5rem;")
            .flex-col.flex-center(style="gap: .5rem;")
                label(for="letters") Letters ( A - Z )
                input#letters(
                    type="text" name="letters" maxlength="10" minlength="3" pattern="[A-Za-z]{3,10}"
                    placeholder="Enter 3 to 10 letters here!" required
                    )
                button(id="submit-btn" type="submit") Unscrabble it! 
            .flex-col.flex-center
                p Search Length ( 3 - 10 )
                .flex-row.flex-center
                    label(for="max") Max: 
                    input#max(type="number" name="max" value="7" max="10" min="3") 
                .flex-row.flex-center
                    label(for="min") Min: 
                    input#min(type="number" name="min" value="4" max="10" min="3") 
